# 6173 历史任务回归扫描（2026-07-27）

## 扫描边界

- 数据源：独立 6173 验证环境的数据副本。
- 范围：19 个已完成且包含采集数据的任务。
- 隐私边界：只统计数量、状态和结构，不输出商品、收件人、快递单号或面单原文。
- 本次只读，没有修改验证数据或现场数据。

## 硬性覆盖结果

| 指标 | 数量 |
| --- | ---: |
| 原始采集记录 | 627 |
| 面单父单 | 1,815 |
| 已识别父单 | 1,815 |
| 子商品结果 | 2,028 |
| 正常结果 | 1,445 |
| 异常结果 | 583 |
| 扫描失败任务 | 0 |
| 零漏单失败任务 | 0 |

19 个任务全部满足：

- 预期面单父单数等于已识别父单数。
- 每条原始采集记录至少关联一个识别结果。
- 每个识别结果都有原始记录来源。
- 所有子商品结果都进入正常或异常分类。

## 已选代表样本

- 多文档原始记录：任务 41 至 61 的全部 19 个已完成任务。
- 多商品拆分：任务 41、42、43、45、46、47、48、49、50、53、54、56、57、58、59。
- 相同内容保留：任务 41、42、43、44、46、47、48、49、50、53、54、56、57、58、59、60。
- 特殊单：任务 41、43、45、46、48、49、53、54、56、57。
- 当前副本未找到可作为固定样本的不可读面单或已匹配但缺图片结果，仍需使用合成回归样本。

## 风险提示

### 任务 41

一个父面单被拆成 3 个子行，其中第 1、3 子行的五个业务字段完全相同。该情况只能
标记为“疑似重复拆分”，不能自动去重，因为同一面单真实包含两条相同商品时仍必须
全部保留。

### 任务 57

单张父面单最多拆出 11 个子行。当前覆盖关系完整，但需要作为规则包拆分上限和
结构核查样本，确认 11 行来自真实商品行而不是解析噪声。

## 已固化的只读守门

隔离分支 `codex/regression-coverage-gate` 已增加：

- 命令：`python scripts/regression_coverage_scan.py`
- 自动化测试：`backend/tests/test_regression_coverage.py`
- 硬失败：父单遗漏、原始采集记录未覆盖、来源丢失、结果未分类。
- 警告：同一父单内完全相同子行、单张父单拆分超过 10 行。
- 警告不能触发自动去重，只用于定位规则包问题。

该命令在同一 6173 副本重新扫描后仍得到 1,815 张预期父面单、1,815 张已识别
父面单、2,028 个子商品结果、1,445 个正常结果和 583 个异常结果；硬失败为 0。
任务 41 和任务 57 分别保留一个重复子行告警和一个高拆分告警。
