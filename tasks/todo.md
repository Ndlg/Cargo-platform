# cargo-platform 整改清单

## P0 纯 AI 自适应验收重做（2026-07-31）

- [x] 将现有 46 条规则标记为 `oracle_seeded_not_ai`，不得计入 AI 成功率
- [x] 备份 6173 平台库、AI 会话库、规则包、镜像和容器清单
- [x] 只清空 6173 规则包、学习记录、AI 会话和派生解析结果
- [x] 核对三轮原始面单、商品、SKU、图片、档口和匹配资产数量不变
- [x] 补齐 6173 当前租户指纹授权与字段选择
- [x] 隔离 5173 标准答案，学习期间后端、AI 和编译器均不可读取
- [x] 页面逐项展示本次实际传给 AI 的原始字段路径、名称和值
- [x] AI 输出严格限定为一个或多个五字段商品行
- [x] 管理员修正保存为学习样本，不直接覆盖业务订单行
- [x] 编译器只从白名单操作生成声明式规则
- [x] 规则必须精确回放确认样本，歧义时不保存并要求第二张样本
- [x] 同格式留出面单自动复用规则，模型调用次数必须为 0
- [x] 覆盖 JSON、XML、嵌套标签、多商品、重复打印、特殊单和不可读样本
- [x] 任务 64、65、66 满足全部面单覆盖守恒
- [x] 完成后才使用独立比较器读取 5173 冻结基准
- [x] 输出 AI 原始正确率、管理员修改率、规则编译率和留出复用正确率
- [x] 已学格式在 AI 服务断开时继续运行
- [x] 6173 人工验收前不合并、不推送、不部署 5173

验收记录：6173 从零规则完成 25 次真实 4B 会话，25 条规则全部编译并回放通过；
模型原始五字段整单正确率为 0/25、字段一致率为 38.26%，因此模型只保留为候选生成器，
规则由管理员五字段样本和确定性编译器生成。任务 64/65/66 共 283 张面单全部覆盖，
生成 293 条商品行，其中 266 条进入正常报货、27 条进入异常；AI 离线时结果不变。
冻结基准仅在整轮结束后由独立比较器读取，未回灌学习链路。

## P0 6173 交付收口（2026-07-30）

- [x] 未生成商品行的父面单逐张进入异常列表和异常 Excel
- [x] 下载前强制校验采集面单覆盖，覆盖不完整时拒绝生成
- [x] 多商品挤进单字段、数量标记混入销售属性时判定解析不完整
- [x] 解析异常按钮进入 AI 面单解析，不进入旧的订单行整理路径
- [x] 导出中心显示覆盖状态，覆盖不完整时禁用下载
- [x] 6173 最近三轮验证 `采集面单 = 正常覆盖 + 异常覆盖`
- [x] 5173 前后容器与运行状态保持不变

## P0 现场收口

- [x] `d131010` 合并并推送到 `origin/main`
- [x] 6173 副本验证任务 61：10 张面单全部覆盖
- [x] 停止采集窗口只替换现场后端
- [x] 部署后验证 5173、后端、识别服务、3 台采集器和数据库
- [x] 保存停止采集后的数据库备份
- [x] 固定四个现场镜像并生成离线镜像包
- [x] 提交并推送现场基线文档和 Git 标签

## P1 回归验收门槛

- [x] 在 6173 副本扫描历史任务，只输出数量和异常类型，不输出业务原文
- [x] 选定多文档、多商品、相同内容、不可读、特殊单、缺图片、重连回放样本
- [x] 建立可重复运行的覆盖矩阵命令
- [x] 增加自动化回归：面单父单零遗漏
- [x] 增加自动化回归：多商品子行不合并
- [x] 增加自动化回归：相同内容不去重
- [x] 增加自动化回归：不可读和特殊单进入异常
- [x] 增加自动化回归：正常行与异常行覆盖所有识别结果

当前扫描结论见 `tasks/regression-scan-20260727.md`。
只读命令：`python scripts/regression_coverage_scan.py`。

## P2 识别规则包

- [ ] 盘点错误合并、错误拆分和不支持平台样本
- [x] 输出当前规则包字段的已生效、未生效和隐藏解析审计
- [x] 将特殊单关键词识别从隐藏硬编码迁移到规则包
- [x] 让数量默认值、字段标签清理、尺码提示、纯属性单和非数字规格五类现有子规则真实生效
- [ ] 确认每种识别行为对应可见规则包规则
- [ ] 移除或登记仍存在的隐藏解析逻辑
- [ ] 规则包修改后自动重跑受影响任务
- [x] 区分缺少规则包、规则包无效、识别服务不可用

## P3 商品、SKU、图片匹配

- [x] 商品规则支持选择多个商品行字段
- [x] 任一所选字段包含关键词即可命中商品
- [x] 未匹配商品只进入异常，不进入正常 Excel
- [x] 只缺图片的已匹配商品仍输出正常 Excel 行
- [ ] 修改匹配规则或资产后自动重跑受影响任务

## P4 采集器可靠性

- [x] 验证掉线期间数据是否完整排队
- [x] 验证重连后只补传未送达数据
- [x] 阻止上一轮面单进入新一轮采集任务
- [x] 记录队列积压、最后成功上传和重连原因
- [x] 未分类轮询异常不再直接退出，记录 `unexpected` 后继续后台重试
- [ ] 处理右边采集器反复掉线的根因

## P5 页面与导出

- [x] 业务页面不再以“第几批”或技术任务号标识面单
- [x] 删除或禁用订单行编辑、确认、排除、审批入口
- [x] 默认展示完整商品行字段，管理员据此维护规则
- [x] 正常 Excel 固定七个业务列
- [x] 异常面单只保留“图片匹配文本”
- [x] 已匹配商品即使缺少可选属性或图片也保留正常 Excel 行
- [x] 验证图片、档口、数量和备注导出不回退

## P6 每次发布必须执行

- [x] 独立分支和 6173 数据副本验证
- [x] 后端全量测试和前端类型检查
- [x] 代表任务覆盖矩阵通过
- [x] 合并前检查 Excel 表头、行数、图片和异常表
- [ ] 现场切换前停止采集并备份数据库
- [x] 固定镜像、Git 标签和回退命令
- [x] 6173 阶段 14：59 个历史轮次零漏单覆盖，页面、接口和日志验收通过
- [ ] 部署后验证健康、采集器、数据库、Excel 和日志
